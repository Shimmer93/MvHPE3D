name: "stage2_cross_camera_joint_residual"

data:
  config: "configs/data/humman_stage2.yaml"

model:
  config: "configs/model/stage2_joint_residual.yaml"

loss:
  pose_6d_weight: 1.0
  betas_weight: 0.1
  joint_weight: 1.0
  init_pose_6d_weight: 0.25
  init_betas_weight: 0.025

optimizer:
  learning_rate: 0.001
  weight_decay: 0.00001

trainer:
  config: "configs/trainer/default.yaml"
