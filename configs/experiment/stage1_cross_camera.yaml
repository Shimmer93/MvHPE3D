name: "stage1_cross_camera"

data:
  config: "configs/data/humman_stage1.yaml"

model:
  config: "configs/model/stage1_mlp.yaml"

loss:
  mhr_weight: 1.0
  shape_weight: 1.0

optimizer:
  learning_rate: 0.001
  weight_decay: 0.0001

trainer:
  config: "configs/trainer/default.yaml"
