name: "stage3_temporal_refine"

data:
  config: "configs/data/humman_stage3.yaml"

model:
  config: "configs/model/stage3_temporal_refine.yaml"

loss:
  pose_6d_weight: 1.0
  betas_weight: 0.0
  joint_weight: 1.0
  articulation_weight: 0.1

optimizer:
  learning_rate: 0.001
  weight_decay: 0.00001

trainer:
  config: "configs/trainer/default.yaml"
